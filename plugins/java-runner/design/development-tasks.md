# Java Snippet Runner 开发任务

## M1：独立工程与交互骨架

- [x] 初始化独立 Vite、React、TypeScript 工程。
- [x] 建立 DevBox 插件清单、分类、图标和中英文标题。
- [x] 使用 CodeMirror 实现 Java 语法高亮。
- [x] 实现运行按钮、快捷键、输出、错误和状态区域。
- [x] 定义结构化执行请求与结果。
- [x] 校验空输入和 64 KiB 输入上限。
- [x] 宿主能力缺失时显示真实提示，不伪造结果。
- [x] 完成构建、类型和单元测试验证。

## M2：受控 JShell Host

- [x] 在 DevBox manifest/SDK 中增加 `java:execute` 权限。
- [x] 增加 Java 执行 IPC 和调用者身份校验。
- [x] 发现并校验 JDK 11+、JShell 可执行文件，并显示当前环境版本。
- [x] 固定参数启动 JShell，不拼接 Shell 命令。
- [x] 实现输入、超时、输出、并发和进程清理限制。
- [x] 覆盖正常输出、语法错误、异常、死循环和身份伪造测试。

## M3：打包与集成验收

- [x] 安装确认正确展示并授权 `java:execute` 权限。
- [x] 生成可重复构建的 ZIP。
- [x] 完成 DevBox 安装、启停、打开、运行和卸载流程。
- [x] 完成中英文和键盘操作验收。
- [x] 更新用户、安全和发布文档。
